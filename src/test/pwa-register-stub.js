// Test-only stand-in for vite-plugin-pwa's virtual:pwa-register/react
// module (see vitest.config.js's resolve.alias and UpdatePrompt.test.jsx).
// Never used at runtime — the real virtual module is generated by the
// VitePWA plugin during the actual Vite build/dev server.
export function useRegisterSW() {
  return {
    needRefresh: [false, () => {}],
    offlineReady: [false, () => {}],
    updateServiceWorker: () => {},
  };
}
