// Global test setup — runs once before the suite. Kept intentionally empty
// besides jest-dom matchers; add global mocks here only when a real need
// shows up (e.g. matchMedia, IntersectionObserver) rather than speculatively.
import '@testing-library/jest-dom/vitest';
